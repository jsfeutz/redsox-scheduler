import { InstallAppContent } from "./install-app-content";

export const metadata = {
  title: "Install app | Rubicon Redsox",
  description: "Add the scheduler to your home screen or desktop.",
};

export default function InstallAppPage() {
  return <InstallAppContent />;
}
